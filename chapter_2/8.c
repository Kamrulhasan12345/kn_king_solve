#include <stdio.h>

int main(void) {
  float amount, rate, payment;
  printf("Enter amount of loan: ");
  scanf("%f",&amount);
  printf("Enter interest rate: ");
  scanf("%f",&rate);
  printf("Enter monthly payment: ");
  scanf("%f",&payment);
  printf("\n");
  printf("Balance remaining after first payment: %0.2f\n",amount=amount*(1+rate/1200.0f)-payment);
  printf("Balance remaining after first payment: %0.2f\n",amount=amount*(1+rate/1200.0f)-payment);
  printf("Balance remaining after first payment: %0.2f\n",amount=amount*(1+rate/1200.0f)-payment);
}